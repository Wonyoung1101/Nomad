package Travel.Itinerary;

public class Itinerary Service {
    public double budgetCalculation(String userId) {
        //calculates the remaining budget
    }

    void onItemChange(String userId, String itineraryItemId) {
        //recalculates the budget when an item is added, removed, or updated
    }
}